- [x] ADR-0001: OWL + owlready2 vs Neo4j (verifies: doc reviewed)
- [x] Model T-Box classes: Territory, TerrainFeature, HydroFeature, Trigger, Exposure, LandslideEvent, HazardAssessment, DataQualityFlag (verifies: `infrastructure.ontology.build` loads and saves without error)
- [x] Object properties: locatedIn, adjacentTo, drainedBy, hasTerrainFeature, exposes, triggeredBy, hasHistoricalEvent, assessedBy (verifies: declared with domain/range in `build.py`)
- [x] Datatype properties matching DB columns exactly (verifies: name diff against `db/models/*.py` columns — canonical_id, official_code, nombre, is_ladera, slope_p90_deg, twi_p90, ndvi_min, hazard_fraction, precip_mm, magnitude)
- [x] Individuals generated from `domain/communes.py` (verifies: `pytest tests/test_ontology.py -q` — count == len(COMMUNES), corregimiento canonical_id/official_code correctly distinct)
- [x] SWRL-sketch mirror of the rule catalog + drift test (verifies: `pytest tests/test_ontology.py::TestOntology::test_swrl_sketch_rule_ids_match_the_rule_catalog -q`). Scope note: `SymbolicRule` individuals carry a human-readable `swrl_sketch` string, generated from `CATALOG`, not an executable owlready2 `Imp`/SWRL rule — the ontology's per-feature class model (slope on `Slope`, not `Territory`) doesn't map cleanly onto flat SWRL bodies without a larger redesign than this spec covered. The drift test still does real work: it fails if `catalog.py` and the ontology's rule listing diverge.
- [x] `infrastructure/ontology/loader.py` (verifies: `ontology_version()` returns a stable 12-char hash, `individuals_for_commune("18")` returns San Cristóbal with official_code "60", `rule_ids()` returns all 8 catalog rule ids)
- [x] Consistency + coverage tests (verifies: `pytest tests/test_ontology.py -q` — 6 passed; full suite 353 passed, 12 skipped, no regressions)
