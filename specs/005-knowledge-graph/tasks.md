- [ ] `db/models/critical_facility.py` + migration — not started; critical facilities live only in the RDF graph (`kg.build.add_critical_facilities`), not persisted to Postgres. Genuinely DB-blocked: no Supabase credentials or local Postgres available in this session to test a migration against.
- [x] Extend the Overpass-based critical-facility query for hospitals/clinics (`kg/build.py::add_critical_facilities`/`_fetch_facilities_near`, verifies: `pytest tests/test_kg_build.py -q`, mocked). Different path than originally planned: instead of extending `alerts/evacuation.py`'s existing safe-zone query, this is a separate function in `kg/build.py` (own concern: territory exposure, not evacuation routing) using the same public, keyless Overpass API. Live-verified working during development (a single-point query for one hospital near a real centroid returned real OSM data); the full 21-commune loop hit the public Overpass instance's aggressive rate-limiting when run back-to-back — added a 1s delay between calls (`delay_s` param, defaults to 1.0, tests pass `delay_s=0`) to respect fair-use, though a full clean 21/21 live run wasn't captured in this session due to the instance's rate limiting.
- [x] `kg/build.py` static A-Box (territory nodes + adjacency) (verifies: `pytest tests/test_kg_build.py -q` — node count matches commune count, no orphan territory)
- [ ] Full Postgres A-Box population (barrio_terrain, barrio_hazard, seismic_events, non-synthetic landslide_events) — not started, needs AsyncSession integration and no DB credentials were available in this session to test against
- [x] True polygon-based adjacency via shapely over the barrio/comuna polygon file (`kg/polygon_adjacency.py`, verifies: `pytest tests/test_polygon_adjacency.py -q`). No DB needed — `platform/frontend/lib/barrios-medellin.json` is a static file already in the repo. Real finding during implementation: the actual GeoJSON has self-intersecting polygon rings that raised a shapely `GEOSException` on `unary_union` until repaired with `buffer(0)` (shapely's standard fix). Honest coverage limit, tested explicitly: the file covers only the 16 urban comunas, not the 5 corregimientos — `kg/build.py::build_static_graph()` uses true polygon adjacency where available and falls back to the old centroid-proximity (k=3 nearest) approximation for the corregimientos, verified by `test_urban_comuna_uses_true_polygon_adjacency_not_capped_at_k` (commune 10 has 10 real neighbors, more than the old k=3 cap) and `test_corregimiento_adjacency_is_bounded_by_k`.
- [x] `upslope_neighbours.sparql` + `run_named_query()` (verifies: `pytest tests/test_kg_build.py -q` — typed results; no longer bounded by `ADJACENCY_K` for the 16 urban comunas now that they use true polygon adjacency)
- [x] `exposed_facilities.sparql` (verifies: same file — returns real facility names once `add_critical_facilities()` has populated the graph)
- [x] `shared_stream_recent_event.sparql` written as the declared target, honestly returning empty against the current DB-free graph (verifies: `test_shared_stream_query_is_honestly_empty_on_the_static_graph` — hydrography and landslide-event data both need Postgres, not faked here)
- [x] `kg/export_cypher.py` optional Neo4j export (verifies: `pytest tests/test_export_cypher.py -q` — well-formed idempotent `MERGE`/`MATCH` statements referencing real territory and adjacency data). Manually verified by running `python -m kg.export_cypher` and inspecting the output; not imported into an actual Neo4j instance in this session (none available).
- [x] Static-graph test suite (verifies: `pytest tests/test_kg_build.py tests/test_polygon_adjacency.py tests/test_export_cypher.py -q` — 23 passed; full suite 377 passed, 12 skipped, no regressions)
