# Dependencias de desarrollo y CI. NO van en la imagen de producción — el
# Dockerfile instala solo requirements.txt.
#
# Uso local:  pip install -r requirements-dev.txt
-r requirements.txt

pytest
pytest-asyncio
ruff

# bandit y detect-secrets los usa ci-security.yml, que los instala por su
# cuenta en pasos explícitos. Se listan aquí para poder correr los mismos
# chequeos en local antes de pushear.
bandit[toml]
detect-secrets
