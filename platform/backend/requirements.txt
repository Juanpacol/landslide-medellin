# Alias de compatibilidad: los workflows de cron (scrapers, predict-risk, etc.)
# y el Dockerfile instalan este archivo. El contenido real vive en
# requirements-base.txt / requirements-prod.txt (runtime) y requirements-dev.txt
# (test/lint, con -r a este mismo archivo). No lo dupliques: si necesitas
# agregar una dependencia de runtime, hazlo en requirements-base.txt.
-r requirements-base.txt
